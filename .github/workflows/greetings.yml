name: Greetings
  
on: [pull_request_target, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    permissions:
      issues: write
      pull-requests: write
    steps:
    - name: test1
      uses: actions/first-interaction@v1
    - name: test2
      run: |
           echo "test"
           mkdir test
           mount
